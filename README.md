# af_back
First make your main API path in the api.js file which available in routes folder,
then after create you own path by creating your own route file,
Every methods implement in controller file, you can create it inside the controller folder,
When you create a schem please make and model file inside model folder.
Database Name: pharmacy
port: 4000
don not change index.js file
